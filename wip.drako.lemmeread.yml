id: wip.drako.lemmeread
runtime: org.gnome.Platform
runtime-version: '45'
sdk: org.gnome.Sdk
command: LemmeRead
modules:
  - name: LemmeRead
    buildsystem: simple
    build-commands:
      - install -D LemmeRead /app/bin/LemmeRead
    sources:
      - type: file
        path: LemmeRead
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --share=network
